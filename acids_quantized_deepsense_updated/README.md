# Deprecated — use deploy package

All deployable artifacts (C code, samples, export scripts) live in:

```text
../ARL_deploy/
```

This folder is kept only as a legacy working copy. Edit and run scripts from the deploy folder instead.
